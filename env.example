# Option A (simple): single values for all envs
MONGO_URI=mongodb://localhost:27017/wms
JWT_SECRET=replace_me_with_strong_secret
# MONGO_URI=mongodb+srv://username:password@cluster0.example.mongodb.net/dbname?retryWrites=true&w=majority

# Option B (recommended): per-environment values
# MONGO_URI_DEV=mongodb://localhost:27017/wms
# MONGO_URI_PROD=mongodb+srv://...
# JWT_SECRET_DEV=replace_me_dev
# JWT_SECRET_PROD=replace_me_prod
PORT=4000

# Auth token settings
# ACCESS_TOKEN_TTL=15m
# REFRESH_TOKEN_TTL=30d
# Required: use a separate secret for refresh tokens (do not reuse JWT_SECRET)
REFRESH_TOKEN_SECRET=replace_me_with_strong_secret
#
# JWT secret must be set
# JWT_SECRET=replace_me_with_strong_secret
#
# Admin signup (development only):
# If set, /api/auth/signup will create an admin when adminCode matches this value.
# ADMIN_SIGNUP_CODE=replace_me

# Web Push (browser push) - VAPID keys
# Generate keys with:
#   npx web-push generate-vapid-keys
# WEB_PUSH_SUBJECT=mailto:admin@example.com
# WEB_PUSH_VAPID_PUBLIC_KEY=replace_me
# WEB_PUSH_VAPID_PRIVATE_KEY=replace_me

# Material low stock threshold (quantity <= threshold triggers notification)
# LOW_STOCK_THRESHOLD=5

# Comma-separated list of allowed browser origins (exact match).
# IMPORTANT: Native iOS/Android apps don't send Origin headers and are automatically allowed.
# CORS_ORIGINS is ONLY needed if you have a web version of your app (browser-based).
#
# For native apps only: Leave CORS_ORIGINS empty or omit it entirely.
# For web apps: Add your frontend domain(s) here.
#
# Example (Option A - simple):
CORS_ORIGINS=http://localhost:8081,http://127.0.0.1:8081,https://your-frontend-domain.com

# Option B (per-environment):
# CORS_ORIGINS_DEV=http://localhost:8081,http://localhost:4000
# CORS_ORIGINS_PROD=https://your-frontend-domain.com
# 
# For iOS/Android native apps: You can leave CORS_ORIGINS_PROD empty or unset it.
# The backend automatically allows requests without Origin headers (native apps).

